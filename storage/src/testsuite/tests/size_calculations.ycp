{
    // overlay ".probe" path

    string probefile = WFM::Args(0);
//    SCR (`RegisterAgent (.probe, probefile));

    // define function

    include "./../include/size_calculations.ycp";

    // run function

    return [calculate_needed_swap(), calculate_needed_boot ("i386", "lilo")];
}

{
    void setup_system(string name)
    {
	SCR::Execute(.target.bash, "mkdir -p tmp");
	SCR::Execute(.target.bash, "rm -rf tmp/*");

	SCR::Execute(.target.bash, sformat("cp data/%1/* tmp/", name));
    }
}
